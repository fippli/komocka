const initialState = {
  port: 8000,
  active: 0,
  endpoints: [
    {
      delay: 0,
      status: 200,
      endpoint: "",
      mock: "{}",
      message: "",
    },
  ],
};

export default initialState;
